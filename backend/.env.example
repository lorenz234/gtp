# Environment Configuration
# Copy this file to .env and fill in the actual values
# Never commit the actual .env file to version control

## API keys & others
# GitHub personal access token for API access
GITHUB_TOKEN=your_github_token_here

# CoinGecko API key for price data
COINGECKO_API=your_coingecko_api_key

# OpenAI API key for AI services
OPENAI_API_KEY=your_openai_api_key

## Database (Postgres) connection - make sure user has write access
DB_HOST=localhost
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password
DB_DATABASE=db_name
DB_PORT=port

## AWS Configuration for S3 and other services
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
S3_CF_BUCKET=your-cloudfront-bucket
S3_LONG_TERM_BUCKET=your-longterm-bucket
CF_DISTRIBUTION_ID=your_cloudfront_distribution_id
