SELECT 
    chain_id, 
    tag_id, 
    COUNT(*) as row_count
FROM public.oli_label_pool_silver
WHERE revoked = FALSE
    AND attester = decode('{{ attester }}', 'hex')
GROUP BY chain_id, tag_id
ORDER BY chain_id ASC, tag_id DESC;